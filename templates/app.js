module.exports = (photos, countdown, related, reviews) => `
<div id="App">${photos}</div>
<div id="CountDown">${countdown}</div>
<div id="Related">${related}</div>
<div id="Reviews">${reviews}</div>
`;